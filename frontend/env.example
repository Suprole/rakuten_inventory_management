# GCS（非公開）からView JSONを読むための設定（A案：Next.js API経由）
#
# 1) GCSのバケット名
GCS_VIEW_BUCKET=rakuten-inventory-views-prod
#
# 2) サービスアカウントJSON（そのまま1行で貼り付け）
#    Vercelの環境変数に設定する想定。ローカルは `.env.local` 等で管理。
GCP_SERVICE_ACCOUNT_JSON=

# GAS WebApp（発注API）をNext.jsサーバーから呼び出すための設定（ブラウザに出さない）
GAS_WEBAPP_URL=
GAS_API_KEY=

# Google Auth（閲覧制限）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
AUTH_ALLOWED_EMAILS=allowed1@example.com,allowed2@example.com
